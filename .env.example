# Browser-visible map key. Restrict this key to your site origins in the map provider.
NEXT_PUBLIC_MAP_KEY=

# Server-only HTTPS endpoint that accepts the JSON lead payload.
# Do not prefix this variable with NEXT_PUBLIC_.
LEAD_WEBHOOK_URL=
